[package]
name = "blyx_bir"
version = "0.1.0"
edition = "2024"
description = "Blyx Intermediate Representation (BIR) Compiler Crate"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
