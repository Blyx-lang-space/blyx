[package]
name = "blyx-analyzer"
version = "0.1.0"
edition = "2024"
description = "Official Language Server Protocol (LSP) implementation for Blyx"

[dependencies]
lsp-types = "0.95"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
